# Iterative Functional Requirements

Small refinements, edge cases, and polish items to address as features are built. This is a bit of a dumping ground of the things that are surfaced once a user has something to react to. The size and scope vary. These need to be regularly reviewed, evaluated and added to the plan. Once theyve been added to the plan, they should be reformatted with a strikethrough.

- Projects need full CRUD
- <br />

* <br />
* select which project when generating a report
* Settings page should be renamed to “projects” and should display a project detail page based on project selected like history and dashboard
* the Log menu option should be a button at the beginning of the horizontal menu, as it immediately goes to an edit page, not a view of the data like all the other menu items. Sign Out should also be a button. 
* Need a detail view of a single log that isn’t the edit screen
* Need to be able to attach a document to a log
* Need a library where any documents uploaded can be viewed
* “Mixed” should also be an option for day quality

- when specific behaviors are logged, the algorithmicly produced result (manic, depressive, mixed,neutral) should overwrite or in someway override the cargivers manual entry on overall mood. And that should be the mood that is displayed on any reports. The manual entry should only be included in any sort of history, dashboard, or report if no other behavioral data is logged. Additionally, when no behaviors are logged there should be an indicator that detailed behavior data wasn’t logged for that day. 
- Projects need more information added to create context for the data being tabulated and visualized. 

  - Family history of mental illness
  - Description
  - Teens interests

    <br />

| <br /> | <br /> | <br /> | <br /> |
| ------ | ------ | ------ | ------ |
| <br /> | <br /> | <br /> | <br /> |
